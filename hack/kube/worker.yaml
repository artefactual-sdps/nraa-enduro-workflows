apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nraa-enduro-worker
  labels:
    app: nraa-enduro-worker
spec:
  serviceName: nraa-enduro-worker
  selector:
    matchLabels:
      app: nraa-enduro-worker
  template:
    metadata:
      labels:
        app: nraa-enduro-worker
    spec:
      serviceAccountName: sdps
      securityContext:
        fsGroup: 1000
      containers:
        - name: nraa-enduro-worker
          image: nraa-enduro-worker:dev
          volumeMounts:
            - name: config
              mountPath: /home/enduro/.config
              readOnly: true
            - name: shared-dir
              mountPath: /home/enduro/shared
          resources: {}
      volumes:
        - name: config
          secret:
            secretName: nraa-enduro-secret
        - name: shared-dir
          persistentVolumeClaim:
            claimName: preprocessing-pvc
